import time
import requests

url = "https://www.stylink.it/rPpyXHQRrvy"

for i in range(20):
    try:
        r = requests.get(url, timeout=10)
        print(f"Test {i+1}/20: {r.status_code}")
    except requests.RequestException as e:
        print(f"Test {i+1}/20: Fehler")

    time.sleep(60)
